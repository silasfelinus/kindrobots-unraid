<?xml version="1.0"?>
<Container version="2">
  <Name>KindRobots</Name>
  <Repository>ghcr.io/silasfelinus/kind_robots:latest</Repository>
  <Registry>https://github.com/silasfelinus/kind_robots/pkgs/container/kind_robots</Registry>
  <Network>cafepurr</Network>
  <MyIP/>
  <Shell>sh</Shell>
  <Privileged>false</Privileged>
  <Support>https://github.com/silasfelinus/kindrobots-unraid/issues</Support>
  <Project>https://github.com/silasfelinus/kind_robots</Project>
  <Overview>Self-hosted Kind Robots Nuxt/Nitro application. GitHub Actions publishes the production image from main to GHCR, so Unraid can detect and install image updates without rebuilding the repo locally. The container reads the existing repo .env at runtime, persistent media stays on the host, and deployment settings remain editable in DockerMan.</Overview>
  <Category>Network:Web</Category>
  <WebUI>http://[IP]:3009</WebUI>
  <TemplateURL>https://raw.githubusercontent.com/silasfelinus/kindrobots-unraid/main/templates/kind-robots.xml</TemplateURL>
  <Icon>https://raw.githubusercontent.com/silasfelinus/kind_robots/main/public/icon-512x512.png</Icon>
  <ExtraParams>--restart=unless-stopped</ExtraParams>
  <PostArgs/>
  <CPUset/>
  <DateInstalled>0</DateInstalled>
  <DonateText/>
  <DonateLink/>
  <Requires>The GHCR package must be public so Unraid can pull it anonymously. Keep the mounted .env private. Publish through an HTTPS reverse proxy; do not expose the application port directly to the internet.</Requires>

  <Config Name="Web Port" Target="3000" Default="3009" Mode="tcp" Description="Host port for the Kind Robots HTTP server. Point the HTTPS reverse proxy at this port." Type="Port" Display="always" Required="true" Mask="false">3009</Config>
  <Config Name="Environment File" Target="/config/kind-robots.env" Default="/mnt/user/appdata/kind_robots/.env" Mode="ro" Description="Existing Kind Robots .env file. The container loads it at runtime, so the full secret environment does not need to be recreated in DockerMan." Type="Path" Display="always" Required="true" Mask="false">/mnt/user/appdata/kind_robots/.env</Config>
  <Config Name="Media Images" Target="/app/.output/public/images" Default="/mnt/user/pc/kindrobots/images" Mode="rw" Description="Persistent Kind Robots image library. The runtime also exposes /app/public as a symlink to the same built public tree for legacy relative paths." Type="Path" Display="always" Required="true" Mask="false">/mnt/user/pc/kindrobots/images</Config>
  <Config Name="Image Storage Path" Target="IMAGES_PATH" Default="/app/.output/public/images" Mode="" Description="Container-visible path for persistent image writes. Overrides any host-specific IMAGES_PATH in the mounted .env file." Type="Variable" Display="always" Required="true" Mask="false">/app/.output/public/images</Config>
  <Config Name="Public App URL" Target="APP_BASE_URL" Default="https://kindrobots.org" Mode="" Description="Canonical public URL used by Kind Robots. Container environment variables override values from the mounted .env file." Type="Variable" Display="always" Required="true" Mask="false">https://kindrobots.org</Config>
  <Config Name="Auth Origin" Target="AUTH_ORIGIN" Default="https://kindrobots.org" Mode="" Description="Canonical authentication origin. Container environment variables override values from the mounted .env file." Type="Variable" Display="always" Required="true" Mask="false">https://kindrobots.org</Config>
</Container>
